---
interface Props {
	date: Date;
	color: string
}

const { date,color } = Astro.props;
---

<time  class={color} datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>