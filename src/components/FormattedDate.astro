---
interface Props {
    date: Date;
}

const { date } = Astro.props;

---

<time datetime={new Date(date).toISOString()}>
    {
        new Date(date).toLocaleDateString('es-mx', {
            year: 'numeric',
            month: 'short',
            day: 'numeric',
        })
    }
</time>