---
interface Props {
    date: Date;
}

const date = new Date(Astro.props.date);
---

<time datetime={date.toISOString()}>
    {
        date.toLocaleDateString('es-mx', {
			year: 'numeric',
            month: 'short',
            day: 'numeric',
        })
    }
</time>